# To use:
#   $ conda env create -f d2l-pytorch-0.17.0.yml  # `mamba` works too for this command
#   $ conda activate d2l-seychelles
name: d2l-seychelles
dependencies:
  - python=3.8
  - pip
  - pip:
    - d2l==0.17.0
    - torch==1.8.1
    - torchvision==0.9.1
